#include "main.h"
#include <string.h>
/**
 * _strlen - Returns the length of a string
 * @s: The string
 * Return: Length of the string
 */
int _strlen(char *s)
{
	/*length is strlen(string)*/
	return (strlen(s));
}
